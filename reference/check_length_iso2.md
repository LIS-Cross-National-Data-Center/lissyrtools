# Checks for the length of the characters in the argument `data`.

Checks the length of the characters in the argument `data`.

## Usage

``` r
check_length_iso2(data)
```

## Arguments

- data:

  A character vector.

## Value

Stops if length of some characters is not 2 or 4, and outputs the
invalid characters.
