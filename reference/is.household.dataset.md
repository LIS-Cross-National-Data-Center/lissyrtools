# Checks if the dataset is at household level

This function checks if there is an 'h' in the name of a LIS or LWS
dataset.

## Usage

``` r
is.household.dataset(dataset_name)
```

## Arguments

- dataset_name:

  The name of a LIS or LWS dataset.

## Value

A boolean.
