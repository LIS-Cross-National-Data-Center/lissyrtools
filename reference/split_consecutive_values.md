# Split vector into consecutive values

Split a vector into groups of consecutive values.

## Usage

``` r
split_consecutive_values(x)
```

## Arguments

- x:

  a vector with values that should be split into groups.
