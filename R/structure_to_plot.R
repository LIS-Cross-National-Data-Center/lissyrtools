


# In here we should code a function that allows to transform the lists from the run_* functions to data frames. 

# possible arguments: 

  #' by = TRUE of FALSE.
  #' probs = TRUE or FALSE, or some of its lenght  ?  the lenght can in the end be deducted, but if share = TRUE or FALSE in run_weighted_percentiles()
  #' database ?   like lis or lws  ? 